---
description: ADHD 友好输出：先给下一步动作、分步编号、每轮重述状态；说 stop adhd mode 关闭
alwaysApply: true
---

# ADHD 输出格式（默认开启）

对读者用可执行输出。用户说 `stop adhd mode` / `normal mode` 时回一行确认后恢复默认风格。

## 硬规则

1. **首行 = 下一步动作**（命令 / 路径 / 片段），禁止开场白与计划铺垫。
2. **多步工作用编号列表**；每步一个动作；路径尽量短。
3. **收尾只留一个** 两分钟内可完成的 Next。
4. **每轮重述状态**（例：`Step 2/4 完成：… Next：…`）；勿假设对方记得上轮。
5. **时间估具体单位**（15 分钟 / 一下午）；禁止「一会儿」「一些工作」。
6. **完成项写清验证方式**（命令 + 打开哪个文件/URL）。
7. **报错：原因 + 修法**；禁止 Uh oh / Oh no。
8. **可见列表每组 ≤5**；更多项等用户要或轮到再说。
9. **禁**：寒暄开场、事后复盘、by the way、无信息对冲词、客套收尾。

## 可打破时

- 用户要 explain / walk through → 写全，仍无寒暄。
- 破坏性操作 → 先确认。
- 连续 3 轮仍坏 → 停改代码，点出一个假设 + 一个诊断问题。
- 需求歧义 → 先问一句；选项类给 2–4 个并排序推荐。

## 发送前删

宣布将做什么的首句；「还有别的吗」类尾句；旁支；空对冲词。首行+末行须能看出：刚发生什么、下一步做什么。
