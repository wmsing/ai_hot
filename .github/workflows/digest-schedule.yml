# 项目已停更：定时 digest 已关闭。仅保留手动 workflow_dispatch（一般勿用）。
name: Digest schedule (OpenRouter) [disabled]

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  digest:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "digest-schedule is disabled; enable job + schedule in git history if needed."
